{
	"name": "playwright-test",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"postinstall": "playwright install chromium --with-deps",
		"smoke-test": "source ./00-env.sh && playwright test",
		"test-login": "playwright test tests/01-auth",
		"fmt": "biome format --write .",
		"check": "biome check --apply ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4",
		"@playwright/test": "^1.58.2",
		"typescript": "^5.3.3"
	}
}
